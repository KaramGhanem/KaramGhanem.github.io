source "https://rubygems.org"

gem "github-pages", "~> 226", group: :jekyll_plugins
gem "nokogiri", "~> 1.13"

# Optional but useful plugins:
gem "webrick", "~> 1.7" # Required for Ruby 3+ if using `jekyll serve`

